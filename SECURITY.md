# 安全策略

## 报告漏洞

请不要通过公开 issue、讨论区或 pull request 报告安全问题。

请使用 GitHub 的私密漏洞报告：在本仓库的 **Security** 页面点击 **Report a vulnerability**。报告中请尽量包含：

- 受影响的组件与版本（提交哈希或版本号）
- 复现步骤或概念验证
- 可能造成的影响

我们会在确认收到后尽快评估，并在修复发布前与你沟通披露时间。

## 支持范围

安全修复只针对 `main` 分支的最新代码和最新发布的版本。

## 部署者须知

- 服务器配置（`backend/config/workflow.server.yaml`）在 `AUTH_JWT_SECRET` 为空或为公开默认值、`BOOTSTRAP_ADMIN_PASSWORD` 为空、过短或为曾公开的默认口令时会拒绝启动；请使用只保存在部署环境中的随机值。
- 数据库端口不要暴露到公网；模型服务凭据只放在服务器环境变量或受限的配置文件中。
- 真实事故材料可能包含个人信息，只应发送给经过授权的模型服务，并按当地法规处理与留存。
