[package]
name = "query_token_accel"
version = "0.1.0"
edition = "2021"

[lib]
name = "query_token_accel"
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
codegen-units = 1
strip = true
